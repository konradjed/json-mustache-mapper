# stop looking up into parent dirs
config.stopBubbling = true

# add Spring’s Qualifier to the list of annotations Lombok will copy onto generated constructors
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier